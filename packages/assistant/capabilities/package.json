{
  "name": "@easyink/assistant-capabilities",
  "type": "module",
  "version": "0.0.24",
  "description": "Pure EasyInk Assistant capabilities for schema, datasource alignment, validation, preview, diff, and patch",
  "author": "hackycy <hackycy@outlook.com>",
  "license": "MIT",
  "publishConfig": {
    "access": "public"
  },
  "sideEffects": false,
  "exports": {
    ".": "./dist/index.mjs",
    "./package.json": "./package.json"
  },
  "types": "./dist/index.d.mts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsdown",
    "dev": "tsdown --watch",
    "test": "vitest run src/**/*.test.ts"
  },
  "dependencies": {
    "@easyink/assistant-plugins": "workspace:*",
    "@easyink/datasource": "workspace:*",
    "@easyink/schema": "workspace:*",
    "@easyink/schema-tools": "workspace:*",
    "@easyink/shared": "workspace:*",
    "zod": "catalog:framework"
  }
}
